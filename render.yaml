services:
  - type: web
    name: rsedunav-api
    runtime: docker
    repo: https://github.com/morewaoj/RSedunav.git
    dockerfilePath: ./artifacts/api-server/Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /api/healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        generateValue: true
